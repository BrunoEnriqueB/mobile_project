import styled from "styled-components/native";
import { Fonts } from "../../theme";

export const InvalidEmailText = styled.Text`
    align-self: flex-end;
    margin-right: 50px;
    font-weight: bold;
    font-family: ${Fonts.robotoR};
    color: #F00;
`;